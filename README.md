# cs-overlay-collection

## Requirements
`node.js`

## Setup
Copy `gamestate_integration_toasthud.cfg` into `CS2_INSTALL_DIR/game/csgo/cfg`

Run `npm install express ws` in this directory

Run `node server.js` from commandline

Add a browser source in OBS with the link you see in commandline (make sure custom CSS is empty)

Enjoy

## TODO
- make weapons not hardcoded
- show defuse kits
- defuse countdown bar
- bomb countdown bar
- clutch box when 1vX
- player positions on radar